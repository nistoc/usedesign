---
usedesign_form: 1
id: demo.session.panel
screen: SessionPanel
presents:
  - field: session-head
    shows: the session's name
  - field: session-head
    shows: the session's phase
---

One anchor, two lines. The inventory can only prove `session-head` once, so the second line is
unverifiable by construction — and whichever line a reader trusts, the other one contradicts it.
Anchors are unique per screen; two meanings for one anchor belong in one line or two anchors.
