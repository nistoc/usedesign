---
usedesign_form: 1
id: demo.registry.page
screen: RegistryPage
maturity: tested
presents:
  - field: registry-head
    shows: the page title
---

A form contract has two maturities, not the card's six: `designed` (written before the screen)
and `implemented` (the default). `tested` borrows a word from the card vocabulary that means
nothing here — a contract has no evidence axis beyond "does the screen render".
