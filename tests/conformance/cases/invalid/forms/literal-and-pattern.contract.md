---
usedesign_form: 1
id: demo.registry.page
screen: RegistryPage
presents:
  - field: kind-tab-all
    field_pattern: 'kind-tab-*'
    shows: the tabs
---

One line names one anchor or one family, never both: two claims on one line cannot be told
apart by the inventory, so the line is refused rather than guessed at.
